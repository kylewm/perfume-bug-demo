import { W } from './constants';
export var roundByTwo = function (num) {
    return parseFloat(num.toFixed(2));
};
export var convertToKB = function (bytes) {
    if (typeof bytes !== 'number') {
        return null;
    }
    return roundByTwo(bytes / Math.pow(1024, 2));
};
/**
 * PushTask to requestIdleCallback
 */
export var pushTask = function (cb) {
    if ('requestIdleCallback' in W) {
        W.requestIdleCallback(cb, { timeout: 3000 });
    }
    else {
        cb();
    }
};
//# sourceMappingURL=utils.js.map