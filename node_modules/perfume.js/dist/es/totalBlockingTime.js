import { fcp, tbt } from './metrics';
export var initTotalBlockingTime = function (performanceEntries) {
    performanceEntries.forEach(function (entry) {
        if (entry.name !== 'self' ||
            entry.startTime < fcp.value) {
            return;
        }
        var blockingTime = entry.duration - 50;
        if (blockingTime > 0) {
            tbt.value += blockingTime;
        }
    });
};
//# sourceMappingURL=totalBlockingTime.js.map