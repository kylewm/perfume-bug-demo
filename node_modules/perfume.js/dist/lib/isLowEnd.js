"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var constants_1 = require("./constants");
exports.getIsLowEndDevice = function () {
    // If number of logical processors available to run threads <= 4
    if (constants_1.getHC() && constants_1.getHC() <= 4) {
        return true;
    }
    // If the approximate amount of RAM client device has <= 4
    if (constants_1.getDM() && constants_1.getDM() <= 4) {
        return true;
    }
    return false;
};
exports.getIsLowEndExperience = function (et, sd) {
    if (exports.getIsLowEndDevice()) {
        return true;
    }
    // If the effective type of the connection meaning
    // one of 'slow-2g', '2g', '3g', or '4g' is !== 4g
    if (['slow-2g', '2g', '3g'].includes(et)) {
        return true;
    }
    // Data Saver preference
    if (sd) {
        return true;
    }
    return false;
};
//# sourceMappingURL=isLowEnd.js.map