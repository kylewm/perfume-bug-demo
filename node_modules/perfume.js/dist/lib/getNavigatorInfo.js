"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var constants_1 = require("./constants");
var getNetworkInformation_1 = require("./getNetworkInformation");
var isLowEnd_1 = require("./isLowEnd");
/**
 * Information coming from window.navigator:
 * 1. Device Memory
 * 2. Hardware Concurency
 * 3. Status of the service worker:
 *     - controlled: a service worker is controlling the page
 *     - supported: the browser supports service worker
 *     - unsupported: the user's browser does not support service worker
 */
exports.getNavigatorInfo = function () {
    if (constants_1.WN) {
        return {
            deviceMemory: constants_1.getDM() || 0,
            hardwareConcurrency: constants_1.getHC() || 0,
            serviceWorkerStatus: 'serviceWorker' in constants_1.WN
                ? constants_1.WN.serviceWorker.controller
                    ? 'controlled'
                    : 'supported'
                : 'unsupported',
            isLowEndDevice: isLowEnd_1.getIsLowEndDevice(),
            isLowEndExperience: isLowEnd_1.getIsLowEndExperience(getNetworkInformation_1.et, getNetworkInformation_1.sd)
        };
    }
    return {};
};
//# sourceMappingURL=getNavigatorInfo.js.map