"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var metrics_1 = require("./metrics");
exports.initTotalBlockingTime = function (performanceEntries) {
    performanceEntries.forEach(function (entry) {
        if (entry.name !== 'self' ||
            entry.startTime < metrics_1.fcp.value) {
            return;
        }
        var blockingTime = entry.duration - 50;
        if (blockingTime > 0) {
            metrics_1.tbt.value += blockingTime;
        }
    });
};
//# sourceMappingURL=totalBlockingTime.js.map